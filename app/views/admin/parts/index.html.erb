<h1>Listing parts</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @parts.each do |part| %>
  <tr>
    <td><%= part.name %></td>
    <td><%= link_to 'Show', [:admin, part] %></td>
    <td><%= link_to 'Edit', edit_admin_part_path(part) %></td>
    <td><%= link_to 'Destroy', [:admin, part], {:confirm => 'Are you sure?', :method => :delete} %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Part', new_admin_part_path %>
