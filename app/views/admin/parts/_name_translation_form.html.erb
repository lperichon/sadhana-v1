<%= f.fields_for :translations, translation do |ft| %>
   <%= ft.label :name %>
   <%= ft.select :locale, I18n.backend.available_locales.collect {|l| [I18n.t('this_file_language', :locale => l), l.to_s]} %><br />
   <%= ft.text_field :name %>
<% end %>