<%= form_for([@practice, @practice.practice_parts.new]) do |f| %>
  <%= f.error_messages %>

  <div class="part field">
      <%= f.label :part, t('practice_part.help.part') %>
      <br/>
      <%= f.collection_select :part_id, Part.find(:all, :order => 'id ASC'), :id, :name %>
</div>
<% end %>
